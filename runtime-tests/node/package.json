{
  "name": "runtime-test-node",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build --entrypoints src/handler.ts --outdir dist --target node --format esm --external @rune/core --external @rune/adapter-node --external @rune/decorators",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rune/adapter-node": "workspace:*",
    "@rune/core": "workspace:*",
    "@rune/decorators": "workspace:*"
  },
  "devDependencies": {
    "@rune/tsconfig": "workspace:*"
  }
}
