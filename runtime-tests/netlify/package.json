{
  "name": "runtime-test-netlify",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build --entrypoints src/handler.ts --outdir dist --target node --format esm --external @rune/core --external @rune/adapter-netlify --external @rune/decorators",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rune/adapter-netlify": "workspace:*",
    "@rune/core": "workspace:*",
    "@rune/decorators": "workspace:*"
  },
  "devDependencies": {
    "@rune/tsconfig": "workspace:*"
  }
}
