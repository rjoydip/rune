{
  "name": "try-vercel",
  "type": "module",
  "scripts": {
    "dev": "vc dev",
    "deploy": "vc deploy",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rune/adapter-vercel": "workspace:*",
    "@rune/core": "workspace:*",
    "@rune/decorators": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@rune/tsconfig": "workspace:*",
    "vercel": "^41.0.0"
  }
}
