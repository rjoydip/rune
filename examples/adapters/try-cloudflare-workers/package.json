{
  "name": "try-cloudflare-workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rune/adapter-cloudflare-workers": "workspace:*",
    "@rune/core": "workspace:*",
    "@rune/decorators": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "@rune/tsconfig": "workspace:*",
    "wrangler": "^4.0.0"
  }
}
