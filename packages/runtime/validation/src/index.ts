/** Validation pipe and error bag for transforming and validating incoming request payloads. */
export { ValidationPipe, ValidationErrorBag } from "./validation-pipe.js";
/** Re-export of class-validator's ValidationError type. */
export type { ValidationError } from "class-validator";
