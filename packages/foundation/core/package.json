{
  "name": "@rune/core",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "bun": "./src/index.ts",
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build:dev": "bun build --entrypoints src/index.ts --outdir dist --external @rune/decorators --no-sourcemap --format esm",
    "build": "bun build --entrypoints src/index.ts --outdir dist --external @rune/decorators --minify --no-sourcemap --format esm",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@rune/container": "workspace:*",
    "@rune/decorators": "workspace:*",
    "@rune/router": "workspace:*",
    "@rune/validation": "workspace:*"
  },
  "devDependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1"
  }
}
